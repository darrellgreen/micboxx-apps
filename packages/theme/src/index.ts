/**
 * @micboxx/theme
 *
 * Shared design tokens for the MicBoxx platform.
 * Provides colors, typography, spacing, radii, and shadows
 * consumed by both the consumer and creator apps.
 */

export { tokens } from './tokens';
